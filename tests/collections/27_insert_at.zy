// Test: $+[i] — insert element at position
// Result is a new array; original is unchanged.

// ── Insert in the middle ────────────────────────────────────────────────────
arr = [10, 20, 30, 40]
arr2 = arr$+[2] 99
>> arr2[0] ¶          // → 10
>> arr2[1] ¶          // → 20
>> arr2[2] ¶          // → 99  (inserted)
>> arr2[3] ¶          // → 30
>> arr2[4] ¶          // → 40
>> arr2$# ¶           // → 5

// ── Insert at beginning (index 0) ──────────────────────────────────────────
arr3 = arr$+[0] 0
>> arr3[0] ¶          // → 0
>> arr3[1] ¶          // → 10
>> arr3$# ¶           // → 5

// ── Insert at end (index = length) ─────────────────────────────────────────
arr4 = arr$+[4] 50
>> arr4[-1] ¶         // → 50
>> arr4$# ¶           // → 5

// ── Original unchanged ──────────────────────────────────────────────────────
>> arr$# ¶            // → 4
>> arr[0] ¶           // → 10
