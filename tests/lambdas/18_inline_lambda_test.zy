// Test inline lambda with map
>> "Testing inline lambda..." ¶

sales = [(100, 5), (250, 3), (75, 10)]

// Use inline lambda
revenues = sales$> (sale -> {
    price = sale[1]
    quantity = sale[2]
    result = price * quantity
    <~ result
})

>> "Revenues: "
@ r:revenues {
    >> r " "
}
¶
